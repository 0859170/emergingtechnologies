<?php
$data = array();

if ($handle = opendir('./data')) {
	
	while (false !== ($entry = readdir($handle))) {
		if ($entry{0} !== "." && is_dir('./'.$entry)) {
			$data[] = $entry;
		}
	}
	
	closedir($handle);
}
?>