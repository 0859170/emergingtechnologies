<?php
$data = array();

if ($handle = opendir('./data')) {
	
	while (false !== ($entry = readdir($handle))) {
		if (stripos($entry,".convert.php")) {
			require_once('./data/'.$entry);
		}
	}
	
	closedir($handle);
}
?>