<?php
if ($handle = opendir('./')) {
	echo "Directory handle: $handle\n";
	echo "Entries:\n";
	
	while (false !== ($entry = readdir($handle))) {
		if ($entry != "." && $entry != ".." && is_dir($entry)) {
			echo "$entry\n";
		}
	}
	
	closedir($handle);
}
?>