<?php
$data = array();

if ($handle = opendir('./data')) {
	
	while (false !== ($entry = readdir($handle))) {
		if ($entry{0} !== "." && is_file('./'.$entry)) {
			$data[] = $entry;
		}
	}
	
	closedir($handle);
}
?>