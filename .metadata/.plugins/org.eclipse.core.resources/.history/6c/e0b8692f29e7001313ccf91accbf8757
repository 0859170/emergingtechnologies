<?php
$data = array();

if ($handle = opendir('./')) {
	
	while (false !== ($entry = readdir($handle))) {
		if ($entry{0} !== "." && is_dir('./'.$entry)) {
			$data["naam"] = $entry;
		}
	}
	
	closedir($handle);
}
?>